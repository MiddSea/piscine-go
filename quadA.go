package piscine

import "fmt"

func QuadA(x, y int) {
	fmt.Println("x, y QuadA")

	//top line
		if x >= 1 && y >= 1 {
			print("o")  // top left corner
		if width > 3 {
			for width := 2 width < x+1; width++ {
				print("-") // top line 
			}
		} 
		print("o") // last
	}
	
	
	//lines according to height 

	//last line
}
